# Итоговый отчет по проекту
## Визуализатор графа зависимостей для Alpine Linux (Вариант №14)

### Общая информация
- **Дата завершения:** 15 ноября 2025 г.
- **Язык программирования:** Python 3.7+
- **Система пакетов:** Alpine Linux (apk)
- **Формат визуализации:** Mermaid / SVG

---

## Выполненные этапы

### ✅ Этап 1: Минимальный прототип с конфигурацией
**Коммит:** `6f80bc2`

**Реализовано:**
- CLI-приложение с использованием argparse
- Парсинг всех требуемых параметров командной строки:
  - `--package` - имя анализируемого пакета
  - `--repo-url` - URL репозитория или путь к тестовому файлу
  - `--test-mode` - режим работы с тестовым репозиторием
  - `--output` - имя выходного SVG файла
  - `--max-depth` - максимальная глубина анализа
  - `--filter` - подстрока для фильтрации пакетов
- Валидация всех параметров с подробными сообщениями об ошибках
- Вывод конфигурации в формате ключ-значение
- Создание структуры проекта (README.md, .gitignore)

**Тестирование:**
- Корректные параметры ✅
- Пустые значения ✅
- Неверное расширение файла ✅
- Отрицательная/чрезмерная глубина ✅
- Отсутствие обязательных параметров ✅

---

### ✅ Этап 2: Сбор данных о зависимостях
**Коммит:** `9181597`

**Реализовано:**
- Загрузка APKINDEX.tar.gz из репозитория Alpine Linux (без сторонних библиотек)
- Ручная распаковка gzip и парсинг tar-архива
- Парсинг формата APKINDEX:
  - Извлечение имен пакетов (строки `P:`)
  - Извлечение зависимостей (строки `D:`)
  - Удаление версионных требований (>=, <, =, !)
- Кеширование данных о пакетах
- Получение прямых зависимостей пакета
- Вывод списка зависимостей на экран

**Тестирование:**
- Пакет `busybox`: 1 зависимость (so:libc.musl-x86_64.so.1) ✅
- Пакет `alpine-base`: 10 зависимостей ✅
- Пакет `musl`: нет зависимостей ✅
- Загружено информации о 5141 пакете из репозитория v3.18

---

### ✅ Этап 3: Построение графа зависимостей
**Коммит:** `bbc06c3`

**Реализовано:**
- Алгоритм DFS без рекурсии (использование стека)
- Построение транзитивного графа зависимостей
- Учет максимальной глубины анализа
- Фильтрация пакетов по подстроке
- Обнаружение и корректная обработка циклических зависимостей
- Режим тестирования с локальными файлами
- Создание тестового репозитория с 26 пакетами
- Вывод полного графа на экран

**Тестовый репозиторий включает:**
- Простые иерархические графы (A → B → C → ...)
- Циклические зависимости (L → N → O → L)
- Линейные цепочки (P → Q → R → ...)
- Графы с общими зависимостями

**Тестирование:**
- Пакет A (max-depth=3): 9 узлов, простая иерархия ✅
- Пакет L (max-depth=5): 13 узлов, циклы обработаны ✅
- Пакет W с фильтром "Y": 3 узла, Y исключен ✅
- Пакет P (max-depth=1): 3 узла, глубина соблюдена ✅

---

### ✅ Этап 4: Обратные зависимости
**Коммит:** `a72da06`

**Реализовано:**
- Алгоритм поиска обратных зависимостей с использованием DFS
- Флаг `--reverse-deps` для включения режима
- Обход всех пакетов в репозитории
- Проверка, зависит ли каждый пакет от целевого
- Учет максимальной глубины и фильтрации
- Вывод списка пакетов, зависящих от данного

**Тестирование:**
- Пакет G (базовая зависимость): 8 обратных зависимостей ✅
- Пакет I (популярная зависимость): 10 обратных зависимостей ✅
- Пакет A (участвует в цикле): 4 обратные зависимости ✅
- Пакет Z с фильтром "X": 2 обратные зависимости (X исключен) ✅
- Пакет P (нет обратных зависимостей): 0 ✅

---

### ✅ Этап 5: Визуализация графа
**Коммит:** `f474b05`

**Реализовано:**
- Генерация кода в формате Mermaid (graph TD)
- Экранирование специальных символов для Mermaid
- Сохранение Mermaid кода в .mmd файлы
- Поддержка Mermaid CLI (mmdc) для генерации SVG
- Fallback: создание SVG с предварительным просмотром и инструкциями
- Создание подробного документа VISUALIZATION_EXAMPLES.md

**Демонстрация на трех пакетах:**

1. **Пакет A** (простой граф):
   - 9 узлов, 3 уровня глубины
   - Классическая иерархия с разветвлениями
   - Файлы: graph_A.mmd, graph_A.svg

2. **Пакет L** (циклические зависимости):
   - 13 узлов, 5 уровней глубины
   - Цикл L → N → O → L
   - Транзитивные зависимости через M → A
   - Файлы: graph_L.mmd, graph_L.svg

3. **Пакет P** (линейная цепочка):
   - 7 узлов, преимущественно линейная структура
   - P → Q, P → R → S → T → U, V
   - Файлы: graph_P.mmd, graph_P.svg

**Сравнение со штатными инструментами:**
- ✅ Полное совпадение прямых зависимостей с `apk info -R`
- ✅ Корректная обработка пакетов busybox, alpine-base, musl
- ✅ Результаты идентичны штатным инструментам apk

**Различия (объяснены в документации):**
- Транзитивный граф vs только прямые зависимости
- Формат Mermaid vs GraphViz DOT
- Встроенная фильтрация и обратные зависимости

---

## Технические характеристики

### Использованные технологии
- **Python 3.13.3** (стандартная библиотека)
- **Модули:** argparse, gzip, io, re, urllib, subprocess
- **Без сторонних зависимостей** для основной функциональности

### Архитектура
```
dependency_visualizer.py
├── DependencyVisualizer (основной класс)
│   ├── __init__() - инициализация параметров
│   ├── print_config() - вывод конфигурации
│   ├── _fetch_apkindex() - загрузка APKINDEX.tar.gz
│   ├── _extract_apkindex_from_tar() - парсинг tar без tarfile
│   ├── _parse_apkindex() - парсинг формата APKINDEX
│   ├── _load_test_repository() - загрузка тестового репозитория
│   ├── get_direct_dependencies() - получение прямых зависимостей
│   ├── build_dependency_graph() - построение графа (DFS без рекурсии)
│   ├── find_reverse_dependencies() - поиск обратных зависимостей
│   ├── generate_mermaid() - генерация Mermaid кода
│   └── save_mermaid_to_svg() - сохранение в SVG
├── validate_arguments() - валидация параметров CLI
├── parse_arguments() - парсинг CLI аргументов
└── main() - главная функция
```

### Алгоритмическая сложность
- **Загрузка APKINDEX:** O(n), где n - размер файла
- **Построение графа (DFS):** O(V + E), где V - узлы, E - рёбра
- **Поиск обратных зависимостей:** O(V * (V + E))
- **Генерация Mermaid:** O(V + E)

---

## Структура репозитория

```
config-valia/
├── .git/                           # Git репозиторий
├── .gitignore                      # Игнорируемые файлы
├── .venv/                          # Виртуальное окружение Python
├── README.md                       # Основная документация
├── VISUALIZATION_EXAMPLES.md       # Примеры и сравнение
├── SUMMARY.md                      # Этот файл
├── dependency_visualizer.py        # Основной исполняемый файл
├── test_repo.txt                   # Тестовый репозиторий (26 пакетов)
├── graph_A.mmd                     # Mermaid код для пакета A
├── graph_A.svg                     # SVG визуализация пакета A
├── graph_L.mmd                     # Mermaid код для пакета L
├── graph_L.svg                     # SVG визуализация пакета L
├── graph_P.mmd                     # Mermaid код для пакета P
└── graph_P.svg                     # SVG визуализация пакета P
```

---

## Git история

```bash
f474b05 (HEAD -> main) Этап 5: Визуализация графа зависимостей
a72da06 Этап 4: Обратные зависимости
bbc06c3 Этап 3: Построение графа зависимостей
9181597 Этап 2: Сбор данных о зависимостях
6f80bc2 Этап 1: Минимальный прототип с конфигурацией
```

Каждый коммит соответствует отдельному этапу разработки и содержит:
- Описание выполненной работы
- Список реализованных функций
- Результаты тестирования

---

## Примеры использования

### 1. Анализ реального пакета Alpine Linux
```bash
python dependency_visualizer.py \
  --package busybox \
  --repo-url https://dl-cdn.alpinelinux.org/alpine/v3.18/main/x86_64 \
  --output busybox_deps.svg \
  --max-depth 5
```

### 2. Тестирование с локальным репозиторием
```bash
python dependency_visualizer.py \
  --package A \
  --repo-url test_repo.txt \
  --test-mode \
  --output test_graph.svg \
  --max-depth 3
```

### 3. Поиск обратных зависимостей
```bash
python dependency_visualizer.py \
  --package G \
  --repo-url test_repo.txt \
  --test-mode \
  --output test.svg \
  --reverse-deps
```

### 4. Фильтрация пакетов
```bash
python dependency_visualizer.py \
  --package musl \
  --repo-url https://dl-cdn.alpinelinux.org/alpine/v3.18/main/x86_64 \
  --output musl.svg \
  --filter dev \
  --max-depth 10
```

---

## Достижения и особенности

### ✅ Выполнены все требования
1. ✅ Не использованы готовые менеджеры пакетов
2. ✅ Не использованы сторонние библиотеки для получения зависимостей
3. ✅ DFS реализован без рекурсии
4. ✅ Учет максимальной глубины
5. ✅ Фильтрация по подстроке
6. ✅ Обработка циклических зависимостей
7. ✅ Режим тестирования
8. ✅ Формат Mermaid
9. ✅ Экспорт в SVG
10. ✅ Три примера визуализации
11. ✅ Сравнение со штатными инструментами
12. ✅ Стандартно оформленные коммиты на каждом этапе

### 🌟 Дополнительные возможности
- Обратные зависимости в одном инструменте
- Подробные сообщения об ошибках
- Кеширование данных репозитория
- Информативные логи процесса работы
- Fallback для SVG без Mermaid CLI
- Подробная документация с примерами

---

## Тестирование

### Покрытие тестовыми сценариями

**Этап 1:**
- ✅ 7 различных сценариев валидации
- ✅ Все типы ошибок обработаны

**Этап 2:**
- ✅ 3 различных пакета Alpine Linux
- ✅ Проверка на пакеты с/без зависимостей

**Этап 3:**
- ✅ 4 сценария с тестовым репозиторием
- ✅ Простые графы, циклы, фильтрация, глубина

**Этап 4:**
- ✅ 5 различных пакетов
- ✅ Базовые зависимости, циклы, фильтрация, пустые результаты

**Этап 5:**
- ✅ 3 различных типа графов
- ✅ Сравнение с apk info -R

---

## Выводы

Инструмент визуализации графа зависимостей для Alpine Linux успешно реализован и протестирован. Все требования задания выполнены:

1. **Функциональность:** Полный набор функций от парсинга CLI до визуализации
2. **Качество кода:** Чистый, документированный Python код
3. **Тестирование:** Подробное тестирование на реальных и тестовых данных
4. **Документация:** Подробные README и примеры
5. **Git история:** 5 коммитов, по одному на каждый этап
6. **Совместимость:** Результаты совпадают со штатными инструментами

### Преимущества разработанного инструмента:
- 🚀 Автоматическое построение транзитивного графа
- 🎨 Современный формат Mermaid
- 🔍 Встроенная фильтрация и обратные зависимости
- 📊 Подробная визуализация с экспортом
- 🛠️ Гибкая настройка глубины и фильтров
- 🧪 Режим тестирования для разработки

**Проект готов к использованию и демонстрации!** 🎉
